<div class="container">
	<form action="/indicator/create" id="indicator_submit" method="POST">
		<h1>Indicator Formation</h1>
		<hr>
		<div class="row">
			<div class="col-md-6">
				<h5>Indicator Code</h5>
				<textarea name="indicator_code" id="indicator_code" class="form-control" rows=5>
function mov_avg(t,k) {
	if (t<k) return;
	var temp = 0;
	for(i=0;i<k;i++) {
		temp = temp + data[t-k].Close;
	}
	return temp/k;
}</textarea>
				<br>
				<h5>Plot Code</h5>
				<textarea name="plot_code" id="plot_code" class="form-control" rows=5>
mov_avg(t,30);</textarea>
				<br>
				<input type="button" id="indicator_plot", value="Plot">
				<input type="button" id="indicator_remove", value="Remove">
				<input type="button" id="stock_refresh" Value="Refresh">
				<input type="button" id="stock_download" Value="Download">
				<div class="btn-group" role="group">
					<button type="button" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
						Configuration
						<span class="caret"></span>
					</button>
					<ul class="dropdown-menu" role="menu">
						<li><input type="text" id="stock_tick" Value=400 size=5> Tick</li>
						<li><input type="text" id="stock_mu" Value=2 size=5> mu</li>
						<li><input type="text" id="stock_sigma" Value=10 size=5> sigma</li>
					</ul>
				</div>
			</div>
			<div class="col-md-6">
				<div id="container" style="height: 400px; min-width: 310px"></div>
			</div>
		</div>
		<hr>
		<button type="button" id="indicator_create" class="btn btn-primary btn-lg btn-block">Create</button>
		
		<!-- Modal -->
		<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
			<div class="modal-dialog">
				<div class="modal-content">
					<div class="modal-header">
						<button type="button" class="close" data-dismiss="modal">
							<span aria-hidden="true">&times;</span><span class="sr-only">Close</span>
						</button>
						<h4 class="modal-title" id="myModalLabel">Create Indicator</h4>
					</div>
					<div class="modal-body">
						<h5>Indicator name</h5>
						<input type="text" id="indicator_name" class="form-control" value="Moving Average" placeholder="Enter indicator name..."/>
						<hr>
						<h5>Description</h5>
						<textarea class="form-control" id="indicator_description" rows="10" placeholder="Enter indicator description...">
This is a simple arithmetic moving average.
The first parameter t stands for time and second parameter k stands for period of averaging backward.
Note that if t smaller than k the code will return 0.</textarea>
					</div>
					<div class="modal-footer">
						<button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
						<input type="submit" id="indicator_save" class="btn btn-primary" value="Save Changes"></input>
					</div>
				</div>
			</div>
		</div>
		<input type="hidden" id="_csrf" name="_csrf" value="<%= _csrf %>" />
		<input type="hidden" id="arthor" name="arthor" value="<%= session.User.name %>" />
	</form>
</div>