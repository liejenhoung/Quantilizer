<div class="container">
	<form action="/indicator/create" method="POST">
		<h1>Indicator Formation</h1>
		<hr>
		<h5>Indicator Name</h5>
		<input type="text" name="indicatorname" class="form-control" value="Moving Average" />
		<hr>
		<h5>Indicator Code</h5>
		<textarea name="indicatorcode" class="form-control" rows=5>
function mov_avg(t,k) {
	if (t<k) return 0;
	var temp = 0;
	for(i=0;i<k;i++) {
		temp = temp + data[t-k].Close;
	}
	return temp/k;
}</textarea>
		<hr>
		<h5>Description</h5>
		<textarea name="indicatordescription" class="form-control" rows=5>
This is a simple arithematic moving average.
The first paramter t stands for time and second parameter k stands for period of averaging backward.
Note that if t smaller than k the code will return 0.</textarea>
		<hr>
		<input type="submit" class="btn btn-lg btn-primary btn-block" value="Create" />
		<input type="hidden" name="_csrf" value="<%= _csrf %>" />
	</form>
</div>